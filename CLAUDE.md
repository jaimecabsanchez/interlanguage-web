# Claude Code — Interlanguage

Las reglas de trabajo de este repo son **compartidas con Codex** y viven en:

- **`AGENTS.md`** (raíz) — visión general, reglas y principios de producto.
- **`plataforma/AGENTS.md`** — la app "Interlanguage HOME" (lo más detallado).
- **`web-publica/AGENTS.md`** — la web pública.

**Trátalos como fuente única de verdad y síguelos.** Si cambias una convención, actualízala en `AGENTS.md`
(no dupliques reglas aquí) para que Codex y Claude sigan alineados.

Recordatorio rápido: sin build · no subir secretos · colores solo con tokens `--il-*` · navegación/iconos desde
`layout.js` · sube `?v=` al cambiar un `.js`/`.css` · demo local `lucia` / `home1234` · corre los tests antes de dar algo por hecho.
